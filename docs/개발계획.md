# KEPCO 배전선로 여유용량 — 통합 개발 계획서

> 최종 수정: 2026-04-14

---

## 1. 시스템 전체 구조

```
[GitHub Actions (3 스레드)]              [웹 서비스]
KEPCO API ──→ Supabase DB 직접 UPSERT ──→ 지도 표시
  (Python, 무료)     (엑셀 중간 단계 제거)    (Next.js/Vercel)

[데스크탑 크롤러] ← 별도 유지 (의뢰자용 독립 도구)
```

---

## 2. 프로젝트 구성

### 2.1 웹 서비스 (web/)
- Next.js + React + TypeScript + Supabase + Tailwind CSS
- Vercel 호스팅 (무료)
- 카카오맵 SDK + 카카오/VWorld 지오코딩

### 2.2 자동 크롤러 (crawler/)
- GitHub Actions (Public repo, 무료 무제한)
- 3개 독립 스레드 동시 실행
- 상세: [CRAWLING.md](./CRAWLING.md)

### 2.3 데스크탑 크롤러 (별도 리포)
- Python + Tkinter, PyInstaller exe 배포
- 의뢰자 직접 사용 독립 도구
- 위치: `project_crawling/kepco여유선로용량/src/`

---

## 3. 비용

| 서비스 | 비용 | 한도 |
|---|---|---|
| Vercel Hobby | $0 | 100GB BW/월 |
| Supabase Free | $0 | 500MB DB, 5GB egress |
| GitHub Actions (Public) | $0 | 무제한 |
| VWorld | $0 | 만료 2026-10-08 |
| Kakao Maps/API | $0 | 300K 호출/일 |
| **합계** | **$0/월** | |

---

## 4. 개발 로드맵

### Phase 1: 웹 서비스 ✅ 완료
- Supabase 셋업, 스키마, 인증/권한
- 엑셀 업로드, 지도 (마커, 클러스터, 필터, 검색, 거리재기, 상세 모달)
- 상세: [PLAN.md](./PLAN.md)

### Phase 2: 자동 크롤링 ✅ 완료
- GitHub Actions 3스레드 크롤링
- flush: UPSERT → 지오코딩 → MV refresh → ref sync → changelog
- 상세: [CRAWLING.md](./CRAWLING.md)

### Phase 3: 웹 관리자 크롤링 제어 ✅ 완료
- CrawlManager UI (시작/중단/모니터링)
- GitHub Actions API 트리거

### Phase 4: 변화 추적 (비교) ✅ 완료
- ref 불변 스냅샷 + changelog 일별 기록
- CompareFilterPanel UI
- 상세: [COMPARE.md](./COMPARE.md)

### Phase 5: UX 폴리싱 ✅ 완료 (2026-04-14)
- 통합 사용 안내 모달 (탭: 주소검색/조건검색/변화추적/지도기능)
- 관리자 페이지 사용 안내 (수집/계정관리) — 태양광 사업자 대상 쉬운 말투
- 지도 이동 공통 헬퍼 `moveMapTo` — 마을/검색/지번 핀 클릭 모두 줌 7 + 애니메이션 통일
- 변화추적 → 지번 클릭 시 상세 모달 자동 필터 + 강조
- 상세 모달 지번 펼침 동시 1개만
- 모바일 날짜 picker: 네이티브 `<input type="date">` 유지 (iOS 호환성)
- 사이드바 헤더 3행 정리 (서비스명 / 사용안내+새로고침 / 사용자+관리자)
- 상세 설정은 `NODE_ENV === "development"` 에서만 노출
- 지번 핀 줌 임계값 8 → 7
- 새로고침 버튼은 window.confirm 으로 남용 방지

### 향후
- [ ] 지적편집도 레이어 + 지도 클릭 시 지번 용량 팝업 (차기 개발 — memory: `project_cadastral_and_click_popup.md`)
- [ ] 줌 레벨별 마커 최적화 (130만 행 대비 — memory: `project_zoom_level_optimization.md`)
- [ ] 태양광 제안서 + 토지 규제 연동 (장기 — memory: `project_solar_proposal.md`)
- [ ] cleanup.yml 에 changelog 정리 추가
- [ ] (선택) 데스크탑 크롤러 Supabase 직접 연동

---

## 4-1. 개발 교훈 (2026-04-14 업데이트)

실제 시행착오에서 얻은 원칙. 신규 작업 시 이 문서 먼저 확인.

### UX / 프론트엔드
- **공통 로직은 반드시 헬퍼로 추출**: 마을/검색/핀 클릭 지도 이동이 3곳에 중복되어 있다가 줌 레벨/애니메이션 변경 시 각각 수정해야 했음. `moveMapTo` 하나로 통일.
- **애니메이션 시 좌표 밀림 방지**: 카카오맵 `setLevel` 직후 `panTo` 를 호출하면 레이아웃 미완료로 중심이 밀림. `setLevel` → `requestAnimationFrame` → `panTo` 순서 필수.
- **React Portal 로 부모 transform 탈출**: 사이드바에 있는 모달이 사이드바 너비에만 갇혔음. `createPortal(..., document.body)` 로 해결.
- **iOS Safari 날짜 picker**: `opacity-0` overlay, `showPicker()` API 등 여러 시도가 모두 실패. 결국 네이티브 `<input type="date">` 가 가장 호환성 좋음.
- **덕지덕지 금지**: 문제가 여러 번 나타나면 각각 패치하지 말고 근본 원인(타이밍/레이아웃/중복 로직)을 찾기.

### 비기술 사용자 대응 (태양광 사업자)
- "GitHub Actions", "워크플로우" 같은 기술 용어 금지. "자동으로 실행됨, 창 닫아도 계속 진행" 식으로 표현.
- 말투는 `~함 / ~가능 / ~권장` 단정형. `~요 / ~세요` 는 피함.
- 개발자용 설정(상세 설정 등)은 `NODE_ENV === "development"` 로 localhost 에서만 노출.
- 새로고침 같은 "부담 큰" 액션은 `window.confirm` 으로 한 번 막기 — 자동 갱신 중임을 안내.

### 데이터 / 성능
- 배포판에서 느린 건 대개 Vercel cold start + Supabase RPC 지연. 쿼리 최적화 전에 cold start 가 원인인지 먼저 확인.
- UNIQUE 제약은 자동으로 B-tree 인덱스 생성됨 (중복 인덱스 추가 금지).

### 좌표 / 지오코딩
- `kepco_addr` = 마을(리) 좌표 (마커 표시용), `geocode_cache` = 지번 좌표 (핀/태양광용). 역할 분리 유지.
- 지번 지오코딩 실패 시 "동+리" → "동만" fallback 필수.

---

## 5. 참고 문서

| 문서 | 내용 |
|---|---|
| [PLAN.md](./PLAN.md) | Phase 1 웹 설계 (인증, 양식 검증, 결정 사항) |
| [CRAWLING.md](./CRAWLING.md) | 크롤링 아키텍처 (스레드, flush, 안전장치) |
| [COMPARE.md](./COMPARE.md) | 변화 추적 (ref, changelog, RPC, 설계 결정) |
| [SERVICES.md](./SERVICES.md) | 외부 서비스 키/콘솔/한도 |
| [요구사항.md](./요구사항.md) | 원본 요구사항 |
