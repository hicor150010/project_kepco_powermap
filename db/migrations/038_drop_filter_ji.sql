-- ══════════════════════════════════════════════
-- 038: get_filter_ji RPC 폐기
-- ══════════════════════════════════════════════
-- 배경:
--   조건검색의 "지번 단위" 탭은 본질적으로 마을 카테고리("전부 여유" 등)
--   기준으로 좁혀진 마을의 지번 전체를 보여주는 구조였음.
--   사용자가 진짜 원하는 "여유 지번 직접 찾기" 와는 다른 의미.
--
--   조건검색을 "마을 찾기" 로 명확히 정의하고, 지번은 마커 클릭 →
--   LocationDetailModal 흐름으로 통일. "지번 단위" 탭 자체 폐기.
--
--   017_filter_ji.sql 의 RPC 는 031 (kepco_capa.addr_id DROP) 이후
--   이미 깨진 상태였으나, 호출처 자체가 사라졌으므로 함수도 제거한다.
--
-- 영향:
--   - /api/filter-ji 폴더 삭제됨
--   - FilterPanel 의 ji 관련 state/UI 모두 제거됨
-- ══════════════════════════════════════════════

DROP FUNCTION IF EXISTS get_filter_ji(text[], integer);
